// Pascal casing for React components
function Message() {
	const name = '';
	if (name) return <h1>Hello {name}</h1>;
	// JSX: Javascript XML syntax, (it's not html, lol)
	return <h1>Hello World</h1>;
}

export default Message;